apiVersion: v2

name: aws-network-topology-operator
description: Handles the setup / configuration of high-level AWS networking to allow cross-VPC communication between clusters

version: "[[ .Version ]]"
appVersion: "[[ .AppVersion ]]"

home: https://github.com/giantswarm/aws-network-topology-operator

# If you have an icon/logo, you should add it to https://github.com/giantswarm/web-assets
# and set the final URL as a value here and uncomment.
#icon: https://s.giantswarm.io/app-icons/example/1/light.svg

annotations:
  application.giantswarm.io/team: hydra
